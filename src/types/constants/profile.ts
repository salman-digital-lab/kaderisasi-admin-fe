export enum USER_LEVEL_ENUM {
  JAMAAH,
  AKTIVIS,
  AKTIVIS_KK,
  KADER,
  KADER_INVENTRA,
  KADER_KOMPROF,
  KADER_LANJUT,
}

export enum GENDER {
  Female = "F",
  Male = "M",
}

export enum ADMIN_ROLE_ENUM {
  SUPER_ADMIN,
  ADMIN,
  KONSELOR = 20,
}
